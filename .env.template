# WARNING: Never commit real tokens.

# BugSnag notifier API key for THIS app (Project Settings -> Notifier API Key).
# This is what the running service uses to report errors to BugSnag.
BUGSNAG_API_KEY=BUGSNAG_API_KEY

# Port the demo service listens on
PORT=3000

# Set to "production" so BugSnag treats events as production for the demo
NODE_ENV=production
